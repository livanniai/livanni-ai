[build]
  command = "npm run build"
  publish = ".next"

# Next.js API route'larının ve sayfalarının Netlify tarafından doğru yakalanması için
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
